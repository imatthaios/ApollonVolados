<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:controls="clr-namespace:ApollonVolados.Mobile.Views.Controls"
    x:Class="ApollonVolados.Mobile.Views.ClubPage"
    Shell.NavBarIsVisible="False"
    BackgroundColor="{StaticResource BackgroundColor}"
    Title="Σύλλογος">

    <ScrollView>
        <VerticalStackLayout Padding="24" Spacing="20">

            <!-- LOGO -->
            <Image
                Source="apollon_logo.png"
                HeightRequest="160"
                HorizontalOptions="Center"
                Aspect="AspectFit" />

            <!-- TITLE -->
            <Label
                Text="Αθλητικός Σύλλογος Απόλλων Βωλάδος"
                Style="{StaticResource TitleLabel}"
                HorizontalTextAlignment="Center" />

            <Label
                Text="Ιδρύθηκε το 1924"
                Style="{StaticResource SmallMutedLabel}"
                HorizontalTextAlignment="Center" />

            <!-- TIMELINE -->
            <Border Style="{StaticResource Card}">
                <Border.Shadow>
                    <Shadow Brush="{StaticResource ShadowColor}" Offset="0,2" Radius="8" />
                </Border.Shadow>

                <VerticalStackLayout Spacing="12">
                    <Label Text="🕰 Χρονολόγιο" Style="{StaticResource SectionHeader}" />
                    <controls:TimelineSection />
                </VerticalStackLayout>
            </Border>

            <!-- CLUB INFO -->
            <Border Style="{StaticResource Card}">
                <Border.Shadow>
                    <Shadow Brush="{StaticResource ShadowColor}" Offset="0,2" Radius="8" />
                </Border.Shadow>

                <VerticalStackLayout Spacing="10">
                    <Label Text="🏟 Στοιχεία Συλλόγου" Style="{StaticResource SectionHeader}" />

                    <Label Text="📅 Έτος Ίδρυσης: 1924" Style="{StaticResource Body}" />
                    <Label Text="🏟 Έδρα: Δημοτικό Γήπεδο Βωλάδος" Style="{StaticResource Body}" />
                    <Label Text="🎨 Χρώματα: Πράσινο – Άσπρο" Style="{StaticResource Body}" />
                </VerticalStackLayout>
            </Border>

            <!-- MILESTONES -->
            <Border Style="{StaticResource Card}">
                <Border.Shadow>
                    <Shadow Brush="{StaticResource ShadowColor}" Offset="0,2" Radius="8" />
                </Border.Shadow>

                <VerticalStackLayout Spacing="12">
                    <Label Text="🏆 Σημαντικοί Σταθμοί" Style="{StaticResource SectionHeader}" />
                    <controls:MilestonesSection />
                </VerticalStackLayout>
            </Border>

            <!-- CONTACT -->
            <Border Style="{StaticResource Card}">
                <Border.Shadow>
                    <Shadow Brush="{StaticResource ShadowColor}" Offset="0,2" Radius="8" />
                </Border.Shadow>

                <VerticalStackLayout Spacing="10">
                    <Label Text="📞 Επικοινωνία" Style="{StaticResource SectionHeader}" />
                    <Button Text="📞 Τηλέφωνο" Style="{StaticResource PrimaryButton}" Command="{Binding CallCommand}" />
                    <Button Text="✉️ Email"     Style="{StaticResource PrimaryButton}" Command="{Binding EmailCommand}" />
                    <Button Text="🌐 Website"   Style="{StaticResource PrimaryButton}" Command="{Binding WebsiteCommand}" />
                    <Button Text="📍 Χάρτης"    Style="{StaticResource PrimaryButton}" Command="{Binding MapCommand}" />
                </VerticalStackLayout>
            </Border>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
