<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:controls="clr-namespace:ApollonVolados.Mobile.Views.Controls"
    x:Class="ApollonVolados.Mobile.Views.ClubPage"
    Title="Σύλλογος"
    BackgroundColor="{StaticResource White}">

    <ScrollView>
        <VerticalStackLayout
            Padding="24"
            Spacing="24">

            <!-- LOGO -->
            <Image
                Source="apollon_logo.png"
                HeightRequest="160"
                HorizontalOptions="Center"
                Aspect="AspectFit" />

            <!-- TITLE -->
            <Label
                Text="Αθλητικός Σύλλογος Απόλλων Βωλάδος"
                FontSize="20"
                FontAttributes="Bold"
                TextColor="{StaticResource PrimaryGreen}"
                HorizontalTextAlignment="Center" />

            <Label
                Text="Ιδρύθηκε το 1924"
                FontSize="13"
                TextColor="{StaticResource Gray600}"
                HorizontalTextAlignment="Center" />

            <!-- TIMELINE -->
            <Border
                BackgroundColor="{StaticResource White}"
                StrokeShape="RoundRectangle 16"
                Padding="16">

                <VerticalStackLayout Spacing="12">
                    <Label
                        Text="🕰 Χρονολόγιο"
                        FontSize="18"
                        FontAttributes="Bold"
                        TextColor="{StaticResource PrimaryGreen}" />

                    <controls:TimelineSection />
                </VerticalStackLayout>
            </Border>

            <!-- CLUB INFO -->
            <Border
                BackgroundColor="{StaticResource White}"
                StrokeShape="RoundRectangle 16"
                Padding="16">

                <VerticalStackLayout Spacing="8">
                    <Label
                        Text="🏟 Στοιχεία Συλλόγου"
                        FontSize="18"
                        FontAttributes="Bold"
                        TextColor="{StaticResource PrimaryGreen}" />

                    <Label
                        Text="📅 Έτος Ίδρυσης: 1924"
                        TextColor="{StaticResource Gray700}" />

                    <Label
                        Text="🏟 Έδρα: Δημοτικό Γήπεδο Βωλάδος"
                        TextColor="{StaticResource Gray700}" />

                    <Label
                        Text="🎨 Χρώματα: Πράσινο – Άσπρο"
                        TextColor="{StaticResource Gray700}" />
                </VerticalStackLayout>
            </Border>

            <!-- MILESTONES -->
            <Border
                BackgroundColor="{StaticResource White}"
                StrokeShape="RoundRectangle 16"
                Padding="16">

                <VerticalStackLayout Spacing="12">
                    <Label
                        Text="🏆 Σημαντικοί Σταθμοί"
                        FontSize="18"
                        FontAttributes="Bold"
                        TextColor="{StaticResource PrimaryGreen}" />

                    <controls:MilestonesSection />
                </VerticalStackLayout>
            </Border>

            <!-- CONTACT -->
            <Border
                BackgroundColor="{StaticResource White}"
                StrokeShape="RoundRectangle 16"
                Padding="16">

                <VerticalStackLayout Spacing="12">
                    <Label
                        Text="📞 Επικοινωνία"
                        FontSize="18"
                        FontAttributes="Bold"
                        TextColor="{StaticResource PrimaryGreen}" />

                    <Button
                        Text="📞 Τηλέφωνο"
                        BackgroundColor="{StaticResource PrimaryGreen}"
                        TextColor="{StaticResource White}"
                        CornerRadius="12"
                        Command="{Binding CallCommand}" />

                    <Button
                        Text="✉️ Email"
                        BackgroundColor="{StaticResource PrimaryGreen}"
                        TextColor="{StaticResource White}"
                        CornerRadius="12"
                        Command="{Binding EmailCommand}" />

                    <Button
                        Text="🌐 Website"
                        BackgroundColor="{StaticResource PrimaryGreen}"
                        TextColor="{StaticResource White}"
                        CornerRadius="12"
                        Command="{Binding WebsiteCommand}" />

                    <Button
                        Text="📍 Χάρτης"
                        BackgroundColor="{StaticResource PrimaryGreen}"
                        TextColor="{StaticResource White}"
                        CornerRadius="12"
                        Command="{Binding MapCommand}" />
                </VerticalStackLayout>
            </Border>

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
