<ContentPage
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="ApollonVolados.Mobile.Views.ContactPage"
    Title="Επικοινωνία">

    <ScrollView>
        <VerticalStackLayout
            Padding="16"
            Spacing="16">

            <!-- HEADER -->
            <Label
                Text="Απόλλων Βολάδας"
                FontSize="22"
                FontAttributes="Bold"
                TextColor="{StaticResource PrimaryGreen}"
                HorizontalTextAlignment="Center" />

            <Label
                Text="Επικοινωνήστε μαζί μας"
                FontSize="14"
                TextColor="{StaticResource Gray700}"
                HorizontalTextAlignment="Center" />

            <!-- PHONE -->
            <Border Style="{StaticResource ContactCard}">
                <Border.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding CallCommand}" />
                </Border.GestureRecognizers>

                <VerticalStackLayout Padding="14" Spacing="4">
                    <Label Text="📞 Τηλέφωνο" Style="{StaticResource ContactTitle}" />
                    <Label Text="+30 69XXXXXXXX" Style="{StaticResource ContactValue}" />
                </VerticalStackLayout>
            </Border>

            <!-- EMAIL -->
            <Border Style="{StaticResource ContactCard}">
                <Border.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding SendEmailCommand}" />
                </Border.GestureRecognizers>

                <VerticalStackLayout Padding="14" Spacing="4">
                    <Label Text="✉️ Email" Style="{StaticResource ContactTitle}" />
                    <Label Text="info@apollonvolados.gr" Style="{StaticResource ContactValue}" />
                </VerticalStackLayout>
            </Border>

            <!-- WEBSITE -->
            <Border Style="{StaticResource ContactCard}">
                <Border.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding OpenWebsiteCommand}" />
                </Border.GestureRecognizers>

                <VerticalStackLayout Padding="14" Spacing="4">
                    <Label Text="🌐 Ιστοσελίδα" Style="{StaticResource ContactTitle}" />
                    <Label Text="www.apollonvolados.gr" Style="{StaticResource ContactValue}" />
                </VerticalStackLayout>
            </Border>

            <!-- LOCATION -->
            <Border Style="{StaticResource ContactCard}">
                <Border.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding OpenMapCommand}" />
                </Border.GestureRecognizers>

                <VerticalStackLayout Padding="14" Spacing="4">
                    <Label Text="📍 Έδρα" Style="{StaticResource ContactTitle}" />
                    <Label Text="Βωλάδα, Κάρπαθος" Style="{StaticResource ContactValue}" />
                </VerticalStackLayout>
            </Border>

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>